# Test Database Configuration
# Uses same database but with test prefix/suffix for isolation
# Tests should use environment variables or override this

mysql.url=jdbc:mysql://localhost:3306/projectlume_test?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&dontTrackOpenResources=true
mysql.username=root
mysql.password=1234
mysql.driver=com.mysql.cj.jdbc.Driver

